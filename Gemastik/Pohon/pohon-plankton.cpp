#include <iostream>
#include <stdio.h>
#include <algorithm>

using namespace std;

main(){
    int n, x, y;

    cin >> n >> x >> y;

    vector<int> temp;
    int a[n], b[n], totalPohon;

    for(int i = 0; i < n; i++){
        bool isFound = false;
        
        cin >> a[i] >> b[i];
        long m = b[i] 
        for(int j = 0; j < temp.size(); j++){
            if()
        }

        if(isFound){
            
        }
    }
    
    cout << pohon(n,x,y,a,b);
}